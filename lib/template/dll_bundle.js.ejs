const path = require('path')
const webpack = require('webpack')

const vendor = <%- JSON.stringify(data.vendor) %>

module.exports = {
  mode: 'production',
  entry: {
    vendor
  },
  output: {
    path: path.resolve('./', 'dll/prod'),
    filename: '[name].js',
    library: '[name]',
  },
  plugins: [
    new webpack.DllPlugin({
      path: path.resolve('./', 'dll/prod/manifest.json'),
      name: '[name]',
      context: process.cwd()
    })
  ]
}