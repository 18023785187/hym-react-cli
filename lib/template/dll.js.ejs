const path = require('path')
const webpack = require('webpack')

const vendor = <%- JSON.stringify(data.vendor) %>

module.exports = {
  mode: 'development',
  entry: {
    vendor
  },
  output: {
    path: path.resolve('./', 'dll/dev'),
    filename: '[name].js',
    library: '[name]',
  },
  plugins: [
    new webpack.DllPlugin({
      path: path.resolve('./', 'dll/dev/manifest.json'),
      name: '[name]',
      context: process.cwd()
    })
  ]
}